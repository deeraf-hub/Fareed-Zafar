# Netlify build settings for the Swabi Jewellers storefront.
# SPA routing is handled by public/_redirects, which ships into dist/.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
