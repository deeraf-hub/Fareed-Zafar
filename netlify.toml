# The storefront lives in web/ — build it from there.
[build]
  base = "web"
  publish = "dist"
  command = "npm run build"

# Single-page app: unknown paths fall back to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
