# react-plotly.js declares a peer dependency on the full "plotly.js" package.
# This app uses the much smaller "plotly.js-cartesian-dist-min" bundle via
# createPlotlyComponent, so we stop npm from auto-installing the full bundle.
# All real peer dependencies are listed explicitly in package.json.
legacy-peer-deps=true
