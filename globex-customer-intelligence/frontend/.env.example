# Base URL the browser uses to reach the API. Leave empty to use relative "/api"
# requests (the Vite dev server and the nginx image both proxy /api to the backend).
VITE_API_BASE_URL=http://localhost:8000
